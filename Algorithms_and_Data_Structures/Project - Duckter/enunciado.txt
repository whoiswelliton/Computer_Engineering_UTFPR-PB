O sistema de submissão é inédito, por isso atente-se aos detalhes agora.

A sua chave de ID é: 53ef8873
A sua chave de AUTENTICAÇÃO é: a12ae7e8

Para enviar um código ao sistema, você deverá, obrigatoriamente, colocar um
define chamado AUTHKEY com a string da sua chave de autenticação. Ou seja, o
seu código deverá OBRIGATORIAMENTE conter a seguinte linha:
```
#define AUTHKEY "a12ae7e8"
```

Cuide muito bem de sua chave de autenticação. Esta chave é que irá
identificar você no sistema. Se esta chave ficar disponível outras pessoas
poderão submeter códigos no seu nome, anulando submissões corretas.

Tendo o seu código fonte com a chave definida você finalmente poderá enviar
o seu código. Para enviar, basta executar o seguinte comando no seu
terminal:
```
nc trinium.naquadah.com.br 65500 < meucodigo.c
```
 - Atente-se de que 'meucodigo.c' deverá ser substituído pelo nome do seu
   arquivo fonte.


CONSULTAS
---------

Além de enviar o seu código por essa interface, você também poderar realizar
algumas consultas.

Gerando uma consulta:

Crie um arquivo com o conteudo:
```
#define AUTHKEY "myauthkey"
#define CONSULTA "minhaconsulta"
```

Substitua o 'myauthkey' pela sua chave de autenticação. Você encontra esta
chave em seu email.

Substitua 'minhaconsulta' pela consulta desejada.

Observe que a 'myauthkey' e a 'minhaconsulta' devem ficar entre aspas.

Exemplo:
```
#define AUTHKEY "a12ae7e8"
#define CONSULTA "nextround"
```

Abaixo segue a lista de consultas disponíveis:

status
------

Mostra o status do sistema. Algumas respostas possíveis são:
 - IDLE - O sistema está recebendo submissões para o próximo round
 - PROCESSING - O sistema está executando um novo round

nextround
---------

Esta consulta mostra o horário em que a próxima execução das submissões será
feita.

currenttable
------------

Esta consulta mostra a tabela de classicação referente a última execução

alltable
--------

Eta consulta mostra todas as tabelas disponíveis

help
----

Mostra esta mensagem.

EOF
