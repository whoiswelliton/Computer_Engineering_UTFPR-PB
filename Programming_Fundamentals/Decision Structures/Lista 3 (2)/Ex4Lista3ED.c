#include <stdio.h>
#include <stdlib.h>

/*
Analise o código seguinte, que tem o objetivo de verificar o valor de variáveis
int main(void)
{
 	  int c;
	//comparação 1
 	  c=10;
 	  if (c != 10 || c != 20 || c!= 30)
 	  {
	  	  printf("\nc != 10 || c != 20 || c!= 30");
	  }
	//comparação 2
  	  c=50;
 	  if (c != 10 || c != 20 || c!= 30)
 	  {
	  	  printf("\nc != 10 || c != 20 || c!= 30");
	  }
	//comparação 3
 	  c=10;
 	  if (c != 10 && c != 20 && c!= 30)
 	  {
	  	  printf("\nc != 10 && c != 20 && c!= 30");
	  }
	//comparação 4
 	  c=50;
 	  if (c != 10 && c != 20 && c!= 30)
 	  {
	  	  printf("\nc != 10 && c != 20 && c!= 30");
	  }
}
	Quais das comparações correspondem ao critério solicitado, ou seja, quais mensagens serão impressas? Por quê?
*/

}
