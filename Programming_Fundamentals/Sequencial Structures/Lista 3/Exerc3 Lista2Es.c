#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
Faça um programa que calcule a quantidade de latas de tinta necessária e o custo para pintar um
tanque cilíndrico. Apresentar o custo com duas casas decimais. Se para a quantidade de latas de
tinta for obtido um resultado fracionário arredondar o número para o próximo inteiro. P
or exemplo: 5.2 apresentar 6. A área a ser pintada é calculada pela fórmula:
AREA = 2 * PI * raio2 + 2 * PI * raio * altura. Uma lata de tinta de 3,6 litros custa R$ 120,00
e que cada litro de tinta é suficiente para pintar três metros quadrados.
Observação: a função int ceil (float num) faz arredondamento do número para cima, ou seja, para
 o próximo inteiro.  É necessário incluir essa biblioteca com # include <math.h>
Exemplo de uso:
Valor = ceil(12.34); ou Valor = ceil(Variável float);
*/

int main (void)
{
    float QuantidadeL,Custo,Area,Raio,Raio2,Altura;
    float PI=3.14

    printf("



    Area = 2 * PI * Raio2 + 2 * PI * Raio * altura

    system("pause");
}
